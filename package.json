{
  "name": "@thewisebunker/shared-types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types, interfaces, DTOs and enums for The Wise Bunker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "~5.7.0"
  }
}
